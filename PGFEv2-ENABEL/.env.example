APP_NAME="PGFE ENABEL"
APP_ENV=local
APP_KEY=base64:e9csuyTWOjixPUeBjUmw1d4wCM2z0E60NId+JdUsziA=
APP_DEBUG=true
# API origin (php artisan serve)
APP_URL=http://localhost:8000
# Vue/Vite SPA origin — must match the browser Origin header (no trailing slash).
# vite.config.ts binds host 127.0.0.1 port 5173 locally.
FRONTEND_URL=http://127.0.0.1:5173
# Domains allowed to use Sanctum stateful cookies (optional; API auth is mainly Bearer).
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:5173,localhost:5174,localhost:4173,127.0.0.1,127.0.0.1:5173,127.0.0.1:5174,127.0.0.1:4173,127.0.0.1:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12
# Si login échoue avec "This password does not use the Bcrypt algorithm" (hashes $2b$), lancer:
#   php artisan pgfe:normalize-bcrypt-hashes
# Ou temporairement : HASH_VERIFY=false (déconseillé en prod)
# HASH_VERIFY=true

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pgfev2
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
# Base URL du backend distant (sans /api/v1/sync à la fin)
SYNC_REMOTE_URL=https://pgfe-back.inafrica.tech
# Token d'API utilisé par SyncService (doit correspondre à un token valide côté distant)
SYNC_API_TOKEN=9|p1sTVxrKE0jVy3Fc1m02eCYBGMrOFGC7m4y7KWvs0ec04131

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
